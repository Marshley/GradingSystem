@model GradingSystem.Models.Grade

@{
    ViewData["Title"] = "AssignSubjectToStudent";
}

<h1>Assign Subject To Student</h1>

<h4>Grade</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="AssignSubjectToStudent">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            @*<div class="form-group">
                <label asp-for="Score" class="control-label"></label>
                <input asp-for="Score" class="form-control" />
                <span asp-validation-for="Score" class="text-danger"></span>
            </div>*@
      
            <div class="form-group">
                <label asp-for="StudentId" class="control-label"></label>
                <select asp-for="StudentId" class="form-control" asp-items="ViewBag.StudentId">
                    @foreach (var std in ViewData["students"] as IList<Student>) 
                    { 
                        <option value="@std.Id">   @std.Firstname @std.Lastname</option>
                    }                            
                 </select>
            </div>
            
            <div class="form-group">
                <label asp-for="SubjectId" class="control-label"></label>
                <select asp-for="SubjectId" class="form-control" asp-items="ViewBag.SubjectId">
                    @foreach(var subj in ViewData["subjects"] as IList<Subject>)
                    {
                        <option value="@subj.Id"> @subj.SubjectName</option>
                    }
                </select>
            </div>
            @*<div class="form-group">
                <label asp-for="FacultyId" class="control-label"></label>
                <select asp-for="FacultyId" class="form-control" asp-items="ViewBag.FacultyId">
                    @foreach (var fac in ViewData["faculties"] as IList<Faculty>)
                    {
                        <option value="@fac.Id"> @fac.LastName</option>
                    }
                </select>
            </div>*@
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="ViewGrade">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
